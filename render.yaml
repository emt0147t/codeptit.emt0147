services:
  - type: web
    name: codeptit-online-judge
    runtime: docker
    plan: free
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: SECRET_KEY
        generateValue: true
